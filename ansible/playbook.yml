---
- name: Tasks file for deploy
  hosts: servers
  become: true
  vars:
    nodejs_path: /var/lib/jenkins/tools/jenkins.plugins.nodejs.tools.NodeJSInstallation/NODEJS/bin
    server_name: http://167.86.100.193
  roles:
    - build
  role:
    - deploy
  tasks:
    - name: Deployment start
      command: echo "Starting deployment..."